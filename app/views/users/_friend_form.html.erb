<% unless current_user?(@user) %>
  <div id="follow_form">
    <% if current_user.f?(@user) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
  </div>
<% end %>