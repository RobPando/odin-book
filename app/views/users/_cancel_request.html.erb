<%= form_for(current_user.requested_friends.find_by(friend_id: @user), html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Cancel request", class: "btn waves-effect waves-light" %>
<% end %>